<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/e99_bg">

    <!-- Animated gradient background matching e99.live sky-motion -->
    <com.estakada99.tv.SkyGradientView
        android:id="@+id/backgroundView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Bouncing logo - position controlled via translationX/Y in code -->
    <ImageView
        android:id="@+id/logoView"
        android:src="@drawable/logo"
        android:layout_width="320dp"
        android:layout_height="80dp"
        android:scaleType="fitCenter"
        android:layout_gravity="top|start" />

    <!-- Status / error text -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="200dp"
        android:text="Connecting..."
        android:textColor="@color/e99_text"
        android:fontFamily="@font/satoshi"
        android:textSize="20sp"
        android:textStyle="bold"
        android:shadowColor="#40000000"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:visibility="visible" />

    <!-- Play/pause - Google TV compliant Button -->
    <Button
        android:id="@+id/playPauseButton"
        android:layout_width="120dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="32dp"
        android:text="▶"
        android:textSize="28sp"
        android:textColor="@drawable/button_text_color"
        android:fontFamily="@font/satoshi"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:background="@drawable/button_focus_selector" />

    <!-- Now playing info - bottom left -->
    <LinearLayout
        android:id="@+id/nowPlayingContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginBottom="28dp"
        android:layout_marginStart="32dp"
        android:orientation="vertical"
        android:maxWidth="480dp">

        <TextView
            android:id="@+id/showNameText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/e99_text"
            android:fontFamily="@font/satoshi"
            android:textSize="16sp"
            android:textStyle="bold"
            android:shadowColor="#20000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:visibility="gone" />

        <TextView
            android:id="@+id/nowPlayingLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Currently playing:"
            android:textColor="@color/e99_text_muted"
            android:fontFamily="@font/satoshi"
            android:textSize="14sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/nowPlayingText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/e99_text"
            android:fontFamily="@font/satoshi"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:singleLine="true"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:shadowColor="#20000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Clock bottom right -->
    <TextClock
        android:id="@+id/clockText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="32dp"
        android:format12Hour="h:mm a"
        android:textColor="@color/e99_text"
        android:fontFamily="@font/satoshi"
        android:textSize="24sp"
        android:shadowColor="#20000000"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="2" />

</FrameLayout>
